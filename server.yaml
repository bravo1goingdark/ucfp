# UCFP Server Configuration
# 
# Increased limits for index operations with large embeddings

bind_addr: "0.0.0.0"
port: 8080

# Increased for large index records with embeddings
timeout_secs: 60
max_body_size_mb: 100  # Increased from 10MB to handle large index records

rate_limit_per_minute: 100

api_keys:
  - "demo-key-12345"

enable_cors: true
log_level: "info"
metrics_enabled: true
